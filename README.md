# Reusable-Project-bootstrap-template
a CLAUDE.md skeleton + the portable .claude/rules/* + a Linear-label setup script + the ops/changelog doc stubs) in a small repo you git clone for every new project — so a new project starts compliant instead of drifting into compliance. 
